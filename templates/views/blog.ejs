<%- include('../partials/header')%>
<%- include('../partials/navbar')%>
<h1>blog page</h1>
<% blogs.forEach((blog)=>{ %>
    <div>
        <h2><%= blog.title %> </h2>
        <img src="<%= blog.image %>" alt="">
        <span><%= blog.date %></span>
        <h2><%= blog.body %></h2>
    </div>
<% }) %> 
<%- include('../partials/footer')%>